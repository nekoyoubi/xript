---
title: Fragment Builder
description: "Interactive demo: build UI fragments, mount them into slots, and see data binding, conditional visibility, and sanitization in action."
---

import FragmentBuilder from '../../../components/FragmentBuilder.astro';

Build fragments, mount them into host slots, and watch `data-bind` and `data-if` respond to live state changes. Try injecting dangerous markup to see the sanitizer strip it in real time.

<FragmentBuilder />
