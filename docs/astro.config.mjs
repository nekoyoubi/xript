// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
	site: "https://nekoyoubi.github.io",
	base: "/xript",
	server: {
		port: 4351,
	},
});
